class Circle {
    init(radius) {
        this.radius = radius;
    }

    area {
        return 3.14 * this.radius * this.radius;
    }
}

/// Getters are inherited.
class C < Circle {}

var circle = C(4);
print circle.area; // 50.24
